# Nginx configuration for custom 404 handling
# Add this to your nginx server block or include it

# Custom error page
error_page 404 /404.html;

# Try to serve the file, if not found, serve 404.html
location / {
    try_files $uri $uri/ /404.html;
}

